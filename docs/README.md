# Screenshots

Replace these placeholders with real screenshots, keeping the exact file
names (the README references them directly):

| File | What to capture |
|---|---|
| `analytics-overview.png` | The analytics overlay (click the bar widget) on the Overview page — donut, provider quota meters and trend visible |
| `popup.png` | The hover popup anchored to the bar widget |
| `settings.png` | Overlay → Settings → Connections, with the provider cards |

Suggested: capture at native resolution, PNG, no browser/OS chrome around
the windows. Crop the overlay shot to the window surface if your compositor
makes full-screen grabs.
